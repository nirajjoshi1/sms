services:
  - type: web
    name: school-management-backend
    runtime: node
    rootDir: server
    buildCommand: npm install --include=dev && npm prune --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
